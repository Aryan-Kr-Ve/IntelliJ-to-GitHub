import java.util.*;

class AyyarSort {
    public static void main(String[] args) {
        String name[] = {"Binod", "Kishor", "Aryan", "Subham"};

        Arrays.sort(name);
        for (int i = 0; i < 4; i++) {
            System.out.println(name[i]);
        }
    }
}
